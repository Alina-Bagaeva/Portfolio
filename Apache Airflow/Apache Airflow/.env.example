# .env.example - КОНФИГУРАЦИОННЫЙ ШАБЛОН
# ==============================================
# КОПИРУЙТЕ ЭТОТ ФАЙЛ В .env И ЗАПОЛНИТЕ ЗНАЧЕНИЯ
# НИКОГДА НЕ КОММИТЬТЕ .env В GIT!
# ==============================================

# === ОБЯЗАТЕЛЬНЫЕ БЕЗОПАСНОСТНЫЕ КЛЮЧИ ===
# Сгенерируйте командой: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=your_generated_fernet_key_here

# Сгенерируйте командой: python3 -c "import secrets; print(secrets.token_hex(32))"
AIRFLOW_SECRET_KEY=your_generated_secret_key_here

# === БАЗЫ ДАННЫХ ===
POSTGRES_PASSWORD=secure_postgres_password_here
REDIS_PASSWORD=secure_redis_password_here

# === ПОЛЬЗОВАТЕЛЬ AIRFLOW ===
# Учетные данные для первого входа в Airflow
_AIRFLOW_WWW_USER_USERNAME=admin
_AIRFLOW_WWW_USER_PASSWORD=secure_airflow_admin_password_here

# === СЕРВЕРНАЯ КОНФИГУРАЦИЯ ===
# IP адрес или домен вашего сервера
DOMAIN_NAME=your-server-ip-or-domain.com

# Пользователь Airflow (обычно 50000 для Linux)
AIRFLOW_UID=50000

# === AIRFLOW НАСТРОЙКИ ===
AIRFLOW__CODE_EDITOR__ROOT_DIRECTORY=/opt/airflow/dags
AIRFLOW__CODE_EDITOR__ENABLED=true
AIRFLOW__CORE__SECURE_MODE=true
AIRFLOW__API__AUTH_BACKENDS=airflow.api.auth.backend.basic_auth
AIRFLOW__WEBSERVER__ENABLE_PROXY_FIX=true
AIRFLOW__WEBSERVER__RBAC=true

# === РЕСУРСЫ ВОРКЕРОВ ===
WORKER_MEMORY_LIMIT=4G
WORKER_CPUS=2
WORKER_MEMORY_RESERVATION=2G

# === ДОПОЛНИТЕЛЬНЫЕ ПЕРЕМЕННЫЕ ===
# Добавьте по необходимости
# AIRFLOW_PROJ_DIR=.
# _PIP_ADDITIONAL_REQUIREMENTS=